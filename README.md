# 社交媒体内容生成助手

基于 LangGraph + Agent Skills 的智能内容生成系统

## 🌟 项目简介

这是一个基于 LangGraph 状态图和 Agent Skills 的智能 Agent 系统，专注于**社交媒体内容生成**。系统采用 **Plan-Execute 模式**，能够根据用户需求自动规划并生成多平台的社交媒体内容，包括抖音、小红书、微博等平台。

### 核心亮点

- 🎯 **Plan-Execute 模式**：智能规划 + 并行执行
- 📱 **多平台支持**：抖音、小红书、微博、Instagram
- 🔄 **三层渐进式披露**：Summary → Detail → Full Context
- 🤖 **Worker 机制**：专业的平台内容生成专家
---

## 🚀 快速开始

### 环境要求

- Python 3.9+
- OpenAI API Key

### 安装步骤

```bash
# 1. 克隆项目
git clone <repository-url>
cd pythonProject

# 2. 创建虚拟环境
python -m venv .venv
source .venv/bin/activate  # Windows: .venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 配置环境变量
cp .env.example .env
# 编辑 .env 文件，填入你的 OPENAI_API_KEY
```

### 运行程序

```bash
python main.py
```

### 使用示例

```
💬 您: 帮我生成一篇关于 iPhone 的小红书种草笔记

🤔 正在生成内容...

============================================================
🤖:
✅ 内容生成完成！

📊 生成结果：
- 抖音短视频脚本：已生成（约685字）
- 小红书种草笔记：已生成（约1216字）
- 微博短文本：已生成（约313字）
- Instagram帖子：已生成（约511字））
============================================================
- 详细内容
......
============================================================
- 质量检查报告
......
```

---

## 📖 使用指南

### 命令行交互

```bash
python main.py
```

**可用命令**：

| 命令 | 说明 |
|------|------|
| `skills` | 查看可用技能 |
| `tools` | 查看可用工具 |
| `debug` | 切换调试模式 |
| `reset` | 重置会话 |
| `exit` | 退出程序 |

---

## 🎨 自定义 Skill

### 创建新的 Worker

1. 在 `meta_agent/skills/content-generate/workers/` 创建新文件：

```markdown
# workers/bilibili_worker.md

你是一位专业的 B 站内容创作专家...

## 输出模板

### 标题
[吸引人的标题]

### 正文
[内容正文]

### 标签
#标签1 #标签2
```

2. 在 `skill_executor.py` 中注册 Worker：

```python
worker_file_map = {
    "@Douyin_Expert": "douyin_worker.md",
    "@Xiaohongshu_Expert": "xiaohongshu_worker.md",
    "@Weibo_Expert": "weibo_worker.md",
    "@Bilibili_Expert": "bilibili_worker.md",  # 新增
}
```

3. 在 `graph_builder.py` 中添加平台名称映射：

```python
platform_names = {
    "@Douyin_Expert": "抖音短视频脚本",
    "@Xiaohongshu_Expert": "小红书种草笔记",
    "@Weibo_Expert": "微博短文本",
    "@Bilibili_Expert": "B站视频脚本",  # 新增
}
```

---

## 🔧 配置说明

### 环境变量（.env）

```bash
# OpenAI API 配置
OPENAI_API_KEY=your_api_key_here
DEFAULT_MODEL=gpt-4
TEMPERATURE=0.7

# 日志级别
LOG_LEVEL=INFO
```

### 调试模式

```bash
# 在命令行中输入
debug

# 或在代码中设置
import logging
logging.getLogger().setLevel(logging.DEBUG)
```

---